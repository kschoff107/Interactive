services:
  # Backend API
  - type: web
    name: code-visualizer-api
    runtime: python
    buildCommand: "pip install -r backend/requirements.txt && cd backend && python -m db.init_db"
    startCommand: "cd backend && gunicorn --bind 0.0.0.0:$PORT app:app"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: code-visualizer-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: JWT_SECRET_KEY
        generateValue: true
      - key: FLASK_DEBUG
        value: "False"
      - key: STORAGE_PATH
        value: "/opt/render/project/src/storage"

  # Frontend Static Site
  - type: web
    name: code-visualizer-frontend
    runtime: static
    buildCommand: "cd frontend && npm install && npm run build"
    staticPublishPath: frontend/build
    envVars:
      - key: REACT_APP_API_URL
        value: https://code-visualizer-api.onrender.com/api
    routes:
      - type: rewrite
        source: /*
        destination: /index.html

# Database
databases:
  - name: code-visualizer-db
    databaseName: code_visualizer
    user: code_visualizer_user
